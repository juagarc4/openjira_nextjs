// Interface does not hav equivalent in Javascript and can not be exported as
// because it will be an empty object. To avoid it we will export the interface
// as  type
export { default as Entry, type IEntry } from './Entry'
